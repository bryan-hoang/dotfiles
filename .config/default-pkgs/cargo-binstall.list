# Manage cargo cache (${CARGO_HOME}, ~/.cargo/), print sizes of dirs and
# remove dirs selectively.
cargo-cache
# The minimal, blazing-fast, and infinitely customizable prompt for any shell
# (starship/starship)!
starship
# 🐢 Magical shell history (ellie/atuin).
atuin
# A cargo subcommand for checking and applying updates to installed
# executables.
cargo-update
# Query crates.io for crates details <https://gitlab.com/imp/cargo-info>
cargo-info
# The next gen ls command (lsd-rs/lsd).
lsd
# A smarter cd command, inspired by z and autojump. It remembers which
# directories you use most frequently, so you can "jump" to them in just a few
# keystrokes.
zoxide
# A simple, fast and user-friendly alternative to 'find'.
fd-find
# A syntax-highlighting pager for git and diff output.
git-delta
# A cat(1) clone with wings.
bat
# A very fast implementation of tldr in Rust.
tealdeer
# A more intuitive version of du in rust.
du-dust
# 🌀 A nonsense activity generator.
genact
# Upgrade everything.
topgrade
# An opinionated Lua code formatter.
stylua
# A cli system trash manager, alternative to rm and trash-cli.
trashy
# A jq clone focussed on correctness, speed, and simplicity.
jaq
# Executes commands in response to file modifications.
watchexec-cli
# Yet another cross-platform graphical process/system monitor.
bottom
# The corrective bash syntax highlighter (anordal/shellharden).
shellharden
# A git tool to manage worktrees and integrate with GitHub and GitLab
# (hakoerber/git-repo-manager).
git-repo-manager
# Fast, configurable, shell plugin manager.
sheldon
# Terminal image viewer.
viu
# Create beautiful image of your source code.
# silicon
# A themeable LS_COLORS generator with a rich filetype datebase (sharkdp/vivid).
vivid
# An implementation of the Language Server Protocol for LaTeX
# (latex-lsp/texlab).
texlab
# A blazing-fast modern Lua linter written in Rust (Kampfkarren/selene).
selene
# An incremental parsing system for programming tools (tree-sitter/tree-sitter).
tree-sitter-cli
# LanguageTool API in Rust (jeertmans/languagetool-rust).
languagetool-rust
# A tool to format codeblocks inside markdown and org documents
# (ukas-reineke/cbfmt).
cbfmt
# Lightning-fast linter for .env files. Written in Rust
# (dotenv-linter/dotenv-linter).
dotenv-linter
# A system information frontend with an emphasis on performance
# (Macchina-CLI/macchina).
macchina
# Filter out duplicates on the command line. Replacement for `sort | uniq`
# (koraa/huniq).
huniq
# Pluggable and configurable code formatting platform written in Rust.
dprint
# A specification for CLIs (jdx/usage).
usage-cli
# 💥 Blazing fast terminal file manager written in Rust, based on async I/O.
yazi-{fm,cli}
# A version manager for neovim (MordechaiHadad/bob).
bob-nvim
# Source code spell checker (crate-ci/typos).
typos-cli
# Modern, performance focused unix commands (SUPERCILEX/fuc).
rmz
# A blazingly fast general purpose fuzzy finder TUI (alexpasmantier/television).
television
# A formatter for kdl documents.
# kdlfmt
# ⚡ A CLI tool for code structural search, lint and rewriting. Written in Rust.
# (ast-grep/ast-grep).
# ast-grep
# 🤖 Just a command runner.
# just
# Another cmake lsp (Decodetalkers/neocmakelsp).
# neocmakelsp
# Background rust code check (Canop/bacon).
# bacon
# A next-generation test runner for Rust (nextest-rs/nextest).
# cargo-nextest
# git commit --fixup, but automatic.
# git-absorb
# A simple and easy to use Wikipedia Text User Interface (Builditluc/wiki-tui).
# wiki-tui
# Ping, but with a graph.
# gping
# A command-line benchmarking tool.
# hyperfine
# 📬 Easily and securely share files from the command line. A fully featured
# Firefox Send client.
# ffsend
# rga: ripgrep, but also search in PDFs, E-Books, Office documents, zip,
# tar.gz, etc.
# ripgrep_all --locked
# An interactive cheatsheet tool for the command-line.
# navi --locked
# Count your code, quickly.
# tokei
# Git repository summary on your terminal.
# onefetch
# A command-line tool and library for generating regular expressions (regex)
# from user-provided test cases.
# grex
# A modern replacement for ps written in Rust.
# procs
# Intuitive find & replace CLI (sed alternative).
# sd
# CSVs sliced, diced & analyzed (jqnatividad/qsv).
# qsv
# A cross-platform CLI csv pretty printer that uses column styling to maximize
# viewer enjoyment.
# tidy-viewer
# A command-line hex viewer.
# hexyl
# Friendly and fast tool for sending HTTP requests.
# xh
# Multi functional app to find duplicates, empty folders, similar images etc.
# czkawka_cli
# A command-line tool to generate, analyze, convert and manipulate colors.
# pastel
# A new way to see and navigate directory trees.
# broot
# Blazing 💥 fast terminal-ui for git written in rust 🦀. (perl in git-bash)
# gitui
# Like jq, but for HTML.
# htmlq
# Command-line interface for email management.
# himalaya
# Launch a local network server with live reload feature for static pages.
# live-server
# A fully functional file server.
# dufs
# Tree command, improved.
# tre-command
# Save space by cleaning non-essential files from software projects.
# kondo
# Command-line JSON viewer designed for reading, exploring, and searching
# through JSON data.
# jless
# A JSON Query Language CLI tool (yamafaktory/jql).
# jql
# A simple tui to view & control docker containers.
# oxker
# Realtime ticker data in your terminal (tarkah/tickrs).
# tickrs
# speedtest-cli in Rust. (nelsonjchen/speedtest-rs).
# speedtest-rs
# Cross-platform ncurses Spotify client written in Rust, inspired by ncmpc and
# the likes (hrkfdn/ncspot).
# ncspot
# A Spotify player in the terminal with full feature parity
# (aome510/spotify-player).
# spotify_player
# A tool to interactively write shell pipelines (elkowar/pipr).
# pipr
# Docuum performs least recently used (LRU) eviction of Docker images
# (stepchowfun/docuum).
# docuum
# A Terminal UI to browse Hacker News (aome510/hackernews-TUI).
# hackernews_tui
# Turn your keyboard into a typewriter! 📇 (orhun/daktilo)
# daktilo
# 🌀 A log file highlighter (bensadeh/tailspin).
# tailspin

# Windows Incompatible.

# Dev tools, env vars, task runner.
# mise
# 🤖 The Modern Port Scanner 🤖.
# https://github.com/RustScan/RustScan/issues/412#issue-1045541309
# rustscan
# Lightweight color picker for X11.
# xcolor
