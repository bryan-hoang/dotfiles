# Fast, disk space efficient package manager (pnpm/pnpm).
pnpm
# Lint commit messages (conventional-changelog/commitlint).
@commitlint/cli
# Installed in `~/package.json` to avoid issues with failing to resolve the
# sharable config package.
# @commitlint/config-conventional
@commitlint/types
# Format all commitlint issues as a simple JSON string
# (bycedric/commitlint-formats).
commitlint-format-json
# Neovim Node.js client and plugin host
neovim
# Opinionated Code Formatter.
prettier
# prettier, as a daemon, for improved formatting speed.
@fsouza/prettierd
# A mighty CSS linter that helps you avoid errors and enforce conventions
# (stylelint/stylelint).
stylelint
# LS for editors.
bash-language-server
yaml-language-server
vscode-langservers-extracted
@olrtg/emmet-language-server
# Autocompletion and go-to-definition for cssmodules
# (antonk52/cssmodules-language-server).
cssmodules-language-server
rome
# A language server for Dockerfiles powered by Node.js, TypeScript, and VSCode
# technologies (rcjsuen/dockerfile-language-server-nodejs).
dockerfile-language-server-nodejs
# MarkdownLint Command Line Interface (igorshubovych/markdownlint-cli).
markdownlint-cli
# A Spell Checker for Code!
cspell
# Makes eslint the fastest linter on the planet (mantoni/eslint_d.js).
eslint_d
# An HTML linter for all markup developers (markuplint/markuplint).
markuplint
# Sort an Object or package.json based on the well-known package.json keys
# (keithamus/sort-package-json).
sort-package-json
# Fast, reliable, and secure dependency management.
yarn
# Find newer versions of package dependencies than what your package.json
# allows.
npm-check-updates
# Check for outdated, incorrect, and unused dependencies (dylang/npm-check).
npm-check
# Check your npm module for unused dependencies (depcheck/depcheck).
depcheck
# CLI tool for running Yeoman generators.
# yo
# Scaffold out a Standard Readme.
# generator-standard-readme
# A language server (implementing the language server protocol) for Svelte.
# svelte-language-server
# The commitizen command line utility.
# commitizen
# A commitizen adapter for the angular preset of
# conventional-changelog/conventional-changelog.
# cz-conventional-changelog
# Command line tool for the Mermaid library.
# @mermaid-js/mermaid-cli
# A service for capitalizing your title properly.
# https://github.com/bryan-hoang/title
# The command line vault.
# @bitwarden/cli
