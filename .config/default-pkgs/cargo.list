# `ccache` with cloud storage (mozilla/sccache).
sccache
# Recursively searches directories for a regex pattern while respecting your
# gitignore.
ripgrep --features pcre2,simd-accel
# A TOML toolkit.
taplo-cli --features lsp
# Beautiful, useful, configurable MOTD generation with zero runtime
# dependencies.
--git https://github.com/rust-motd/rust-motd.git
# Download an asset from the latest GitHub release.
--git https://github.com/devmatteini/dra
# Intercept any risky patterns (default or defined by you) and prompt you a
# small challenge for double verification.
--git https://github.com/kaplanelad/shellfirm shellfirm
# A command-line DNS client.
# --git https://github.com/ogham/dog dog
# An over-engineered rewrite of pipes.sh in Rust.
# --git https://github.com/lhvy/pipes-rs
# A general purpose application launcher for X and wayland inspired by
# rofi/wofi and alfred (onagre-launcher/onagre).
# --git https://github.com/onagre-launcher/onagre

# Windows Incompatible.

# A command-line tool to prevent committing secret keys into your source code.
--git https://github.com/sirwart/ripsecrets --branch main
# A lightning fast version of tmux-fingers written in Rust, copy/pasting tmux
# like vimium/vimperator (fcsonline/tmux-thumbs).
--git https://github.com/fcsonline/tmux-thumbs thumbs
# xautolock rewrite in Rust, with a few extra features (jD91mZM2/xidlehook).
# xidlehook --bins
