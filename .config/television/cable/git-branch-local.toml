[metadata]
description = "A channel to select from git branches"
name = "git-branch-local"
requirements = ["git"]

[source]
command = "git --no-pager branch --format=\"%(refname:short)\""
output = "{split: :0}"

[preview]
command = "git show -p --stat --pretty=fuller --color=always '{0}'"
