[[regexes]]
# Ethereum addresses. `dcp-worker` truncates them down to 20 characters long.
regex = '(?:0x)?[0-9a-fA-F]{20,40}'
style = { fg = "blue" }

[[keywords]]
style = { fg = "red" }
words = ['Error', 'error']
